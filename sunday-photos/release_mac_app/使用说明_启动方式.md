# macOS 启动方式（老师版 .app）

1) 双击  启动。
2) 默认会自动打开“终端 Terminal”窗口显示运行进度（控制台信息）。

可选（高级/静默模式）：
- 重新生成 app 时设置 ，可让程序后台运行、不打开终端。
- 静默模式下请查看工作目录  里的日志。

如果 macOS 提示“无法打开/来自未知开发者”：
- 系统设置 → 隐私与安全性 → 找到被拦截的 app → 仍要打开。
